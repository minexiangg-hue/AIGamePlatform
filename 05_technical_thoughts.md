# AI Game Platform — 技术方案思考

> 最后更新: 2026-06-03
> 状态: 已定架构 + Agent 选型分析

## 架构决策（已定）

| 决策 | 结论 |
|------|------|
| 运行格式 | HTML5（浏览器直接运行） |
| 开发界面 | 自研 Prompt + 模板填空混合界面 |
| 代码分层 | **设计层**（用户设定/参数/结构） + **代码层**（Agent 生成的源码） |
| 开源权限 | 设计层和代码层可独立设置开源/闭源 |

---

## 代码分层设计

### 两层结构

```
用户创作
    │
    ├── 设计层（浅层） ← 用户拥有
    │   ├── Prompt 历史（用户的描述、修改要求）
    │   ├── 模板选择 + 填空参数
    │   ├── 游戏结构设计（关卡、规则、数值）
    │   ├── 美术风格设定（像素风/手绘/3D等）
    │   └── 开源权限：用户可选择是否公开
    │
    └── 代码层（深层） ← Agent 生成
        ├── HTML5/JS 源码
        ├── 资源文件（图片、音效等）
        └── 开源权限：跟随设计层，也可独立设置
```

### Fork 的含义

Fork 不是复制源码，而是**复用设计层**：

```
用户 A 开源了他的设计层
    │
    └── 用户 B Fork
            │
            ├── B 获得 A 的设计设定（关卡结构、数值、风格）
            ├── B 的 Agent 基于这些设定重新生成代码
            │   （生成的代码与 A 的不同，但游戏设计相似）
            └── B 可在 A 的设计基础上修改和发布自己的版本
```

如果代码层也开源，别人还可以直接查看/修改源代码，但这不是 Fork 的必要条件。

---

## AI Agent 选型分析

### 核心需求

| 需求 | 优先级 | 说明 |
|------|--------|------|
| 适配自研开发界面 | 必选 | Agent 需要嵌入我们的 Prompt + 模板混合界面 |
| 持续编写和修改代码 | 必选 | 多轮迭代修改游戏代码，不是一次生成完事 |
| 生图能力 | 可选 | 辅助生成游戏素材（像素画、UI、背景等） |
| 成本可控 | 重要 | MVP 阶段不能烧太多钱 |
| 可本地化/自部署 | 中期 | 用户量大后需考虑成本优化 |

### 候选方案对比

| 方案 | 代码能力 | 生图 | 可嵌入性 | 成本 | 推荐度 |
|------|---------|------|---------|------|--------|
| **Claude API (Sonnet)** | ⭐⭐⭐⭐⭐ | ❌ | API 调用，高 | 中-高 | ⭐ MVP 首选 |
| **Claude Code（协议分析）** | ⭐⭐⭐⭐⭐ | ❌ | CLI 工具，不适合嵌入网页 | 中-高 | ❌ 不推荐 |
| **OpenClaw** | ⭐⭐⭐ | ❌ | 可自部署 | 低 | ⭐ 备选 |
| **Hermes (Nous)** | ⭐⭐⭐ | ❌ | 可自部署 | 低 | ⭐ 备选 |
| **Qwen2.5-Coder** | ⭐⭐⭐⭐ | ❌ | 可自部署 | 低 | ⭐⭐ 成本优化首选 |
| **Stable Diffusion / FLUX** | — | ⭐⭐⭐⭐ | API/自部署 | 中 | ✅ 生图专用 |

### 推荐方案

**MVP 阶段：Claude API (Sonnet) + Stable Diffusion**

| 组件 | 方案 | 理由 |
|------|------|------|
| 核心代码 Agent | **Claude API (Sonnet)** | 代码能力当前最强；多轮修改最稳定；API 调用易嵌入自研界面 |
| 生图 | **Stable Diffusion (本地/API)** | 开源成熟；游戏素材生成够用；可后期换成 FLUX |
| 模板渲染 | **自研** | 设计层→代码层的映射不需要大模型参与 |

**为什么不是其他方案：**

- **Claude Code 扒源码** — 即使能拿到协议/源码，它是 CLI 工具，架构上不适合嵌入网页端开发界面。我们更需要 API 层的灵活性
- **Hermes / OpenClaw** — 开源模型里不错的，但与小游戏代码生成和迭代修改的能力相比，还是不如顶级闭源 API。可以作为**第二阶段成本优化的替代方案**
- **Qwen2.5-Coder** — 开源编码模型里最强的之一，可作为 MVP 后的降本方案

**第二阶段（放量后）：混合架构**

```
用户请求 → 路由层
              ├── 简单生成/模板填充 → 本地开源模型（Hermes/Qwen）→ 低成本
              └── 复杂生成/大改 → Claude API → 高质量
```

这个路由层可以大幅降低成本，同时保持质量。

### Agent 架构示意

```
┌─────────────────────────────────────┐
│          自研开发界面                │
│  [Prompt 输入区]  [模板选择面板]     │
└──────────┬──────────────────────────┘
           │ 用户输入
           ▼
┌─────────────────────────────────────┐
│         Agent Orchestrator          │
│  - 意图理解（用户想做什么类型的游戏）│
│  - 设计层提取（关卡/规则/数值）      │
│  - 模板匹配或代码生成                │
│  - 多轮迭代管理                      │
└──────────┬──────────────────────────┘
           │
     ┌─────┴─────┐
     ▼           ▼
┌─────────┐ ┌─────────┐
│ Claude  │ │   SD    │
│  (代码)  │ │  (生图)  │
└────┬────┘ └────┬────┘
     │           │
     └─────┬─────┘
           ▼
┌─────────────────────────────────────┐
│        代码沙箱（预览/测试）          │
│  - 实时预览 HTML5 游戏              │
│  - 自动检测运行错误                  │
│  - 一键发布                        │
└─────────────────────────────────────┘
```

## 关键风险

| 风险 | 概率 | 缓解方案 |
|------|------|---------|
| AI 生成的游戏质量太差 | 中 | 平台种子游戏设定质量标准基线；Agent 持续迭代 |
| 生成成本过高 | 中 | 优化 prompt，缓存常用模板；付费 tier 覆盖成本；后期切混合架构 |
| 版权问题（生成物与现有游戏雷同） | 低-中 | 生成时加去重检测；用户协议明确责任归属 |
| 沙箱安全（恶意代码） | 中 | 所有生成代码在隔离容器中运行；输出审查 |
| 开源模型替代进度 | 低 | 持续跟踪 Qwen/Hermes 等模型进展，随时准备切换 |
